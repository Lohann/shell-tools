# Shell Utils

Collection of portable shell scripts.

## Design Goals
- **Portable:** Works in any posix compatible shell (`bash`, `dash`, `ksh`, `zsh`, etc..).
- **Compatible**: Probe the system to use only portable features.
- **System Agnostic:** Works in any posix compliant system, including **Linux**, **MacOS**, **Windows WSL**, etc...
